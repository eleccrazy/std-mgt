import { StudentDataRegister } from 'interfaces/student';
const excelProcessor = (data: StudentDataRegister[]) => {
  // Excel Processor
  return { message: 'File processed successfully' };
};

export default excelProcessor;
